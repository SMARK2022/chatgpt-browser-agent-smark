This directory is intentionally empty.

Older experiments stored ChatGPT browser runtime state here. The current default
uses the user-level opencode data directory; the local OpenCode config relies on
that default rather than this checkout-local directory.

Do not store `daemon.json`, browser profiles, project caches, or session indexes
inside this checkout.
